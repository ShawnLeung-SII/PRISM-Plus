# PRISM+ Stage 1 — SpatialBND (C1: Spatial-SPR cross-attention)
# Hardware target: 4× NVIDIA H200 SXM 141GB

# --- data ---
data_root: /inspire/hdd/project/robot-dna/liangxiujian-253308390319/ByteCameraDepth
resolution: 512
num_workers: 8

# --- VFM backbone ---
vfm_type: moge2
vfm_checkpoint: /inspire/hdd/global_user/liangxiujian-253308390319/0-XIUJIANLIANG/weights/moge2.pt
encoder_size: large

# --- training ---
batch_size: 32          # per-GPU, effective batch = 32 * world_size
epochs: 100
lr: 1.0e-4
pos_weight: 3.0
dice_weight: 0.3

# --- output ---
output_dir: /inspire/hdd/global_user/liangxiujian-253308390319/0-XIUJIANLIANG/checkpoints/prism_plus/stage1
